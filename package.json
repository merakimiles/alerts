{
  "name": "miles",
  "version": "0.1.0",
  "private": true,
  "description": "Miles — Meraki Webhook Receiver + Alerts UI",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w packages/miles-api\" \"npm run dev -w packages/miles-web\"",
    "build": "npm run build -w packages/miles-api && npm run build -w packages/miles-web",
    "start": "concurrently -n api,web -c blue,green \"npm run start -w packages/miles-api\" \"npm run start -w packages/miles-web\"",
    "db:migrate": "npm run db:migrate -w packages/miles-api",
    "db:reset": "npm run db:reset -w packages/miles-api",
    "db:studio": "npm run db:studio -w packages/miles-api",
    "seed:samples": "npm run seed:samples -w packages/miles-api",
    "lint": "echo \"No linter configured\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

